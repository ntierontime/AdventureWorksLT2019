export enum CrudViewContainers {
    None = 'None',
    StandaloneView = 'StandaloneView',
    Dialog = 'Dialog',
    // Inline = 'Inline', // can't EDIT in a data table or tiles. must be a Dialog or Page
    Card = 'Card', // when Tile
}
