@using AdventureWorksLT2019.Models;
@using AdventureWorksLT2019.MvcWebApp.Models;
@using AdventureWorksLT2019.Resx;
@using Framework.Models;
@model ProductModelProductDescriptionCompositeModel;
@inject ViewFeaturesManager viewFeatureManager;
@inject DashboardViewModelHelper dashboardViewModelHelper;
@inject MvcItemViewModelHelper mvcItemViewModelHelper;
@inject IUIStrings localizor;
@{
    ViewData["Title"] = localizor.Get("Dashboard");
    var routeId = Model.__Master__.ProductModelID;
    var masterVM = await dashboardViewModelHelper.GetProductModelProductDescriptionMvcItemViewModel(Model.__Master__, Model.Get(ProductModelProductDescriptionCompositeModel.__DataOptions__.__Master__));
}
<div class="row">
    <div class="col-12 col-md-8 nopadding">
        <ul class="nav nav-tabs" id="dashboardTabs" role="tablist">
        </ul>
        <div class="tab-content" id="dashboardTabContent">
        </div>
    </div>
    <div class="col-12 col-md-4 col-lg-3">
        <!-- ProductModelProductDescriptionCompositeModel.__DataOptions__.__Master__ -->
        <div class="nt-list-wrapper"
             id="master"
             data-nt-bs-modalsize="Large"
             data-nt-loaditem-url="@Url.Action("AjaxLoadItem", "ProductModelProductDescription")"
             data-nt-createitem-url="@Url.Action("AjaxCreate", "ProductModelProductDescription")"
             data-nt-updateitem-url="@Url.Action("AjaxEdit", "ProductModelProductDescription")"
             data-nt-deleteitem-url="@Url.Action("AjaxDelete", "ProductModelProductDescription")">
            <div class="nt-listitem" data-nt-route-id="@routeId">
                @await Html.PartialAsync("~/Views/ProductModelProductDescription/_TileDetailsItem.cshtml", masterVM)
            </div>
        </div>
        <!-- ProductModelProductDescriptionCompositeModel.__DataOptions__.__Master__ -->
    </div>
</div>
<partial name="~/Views/Shared/_CrudActionDialog.cshtml" />
<partial name="~/Views/Shared/_SimpleActionConfirmationDialog.cshtml" />
@section Scripts
    {
    <script>
        $(document).ready($(function () {
        }))
    </script>
}
