@using AdventureWorksLT2019.Models;
@using AdventureWorksLT2019.Resx;
@using Framework.Models;
@model PagedViewModel<BuildVersionDataModel[]>
@inject IUIStrings localizor;
@if (Model != null && Model.Result != null && Model.Result.Status == System.Net.HttpStatusCode.OK && Model.Result.ResponseBody != null)
{
    <!-- Carousel -->
    <div id="slideshow-buildversion" class="carousel carousel-dark slide" data-bs-ride="carousel">
        <!-- Indicators/dots -->
        <div class="carousel-indicators">
            @for (var i = 0; i < Model.Result.ResponseBody.Length; i++)
            {
                <button type="button" data-bs-target="#slideshow-buildversion" data-bs-slide-to="@i" class="@(i == 0 ? "active" : "")"></button>
            }
        </div>
        <!-- The slideshow/carousel -->
        <div class="carousel-inner">
            @for (var i = 0; i < Model.Result.ResponseBody.Length; i++)
            {
                <div class="carousel-item@(i == 0 ? " active" : "")">
                    <img src="images/Background@(i.ToString("d2")).png" alt="@Model.Result.ResponseBody[i].Database_Version" class="d-block w-100">
                    <div class="carousel-caption d-none d-md-block">
                        <h5 class="card-title">@Model.Result.ResponseBody[i].Database_Version</h5>
                        <h6 class="card-subtitle mb-2 text-muted">@Model.Result.ResponseBody[i].Database_Version</h6>
                        <p class="card-text">@Model.Result.ResponseBody[i].Database_Version</p>
                        <a asp-area="" asp-controller="BuildVersion" asp-action="Dashboard" asp-route-systeminformationid="@Model.Result.ResponseBody![i].SystemInformationID" asp-route-versiondate="@Model.Result.ResponseBody![i].VersionDate" asp-route-modifieddate="@Model.Result.ResponseBody![i].ModifiedDate" title="@Model.Result.ResponseBody[i].Database_Version">
                             @Model.Result.ResponseBody[i].Database_Version
                        </a>
                    </div>
                </div>
            }
        </div>
        <!-- Left and right controls/icons -->
        <button class="carousel-control-prev text-black" type="button" data-bs-target="#slideshow-buildversion" data-bs-slide="prev">
            <span class="carousel-control-prev-icon text-black"></span>
        </button>
        <button class="carousel-control-next text-black" type="button" data-bs-target="#slideshow-buildversion" data-bs-slide="next">
            <span class="carousel-control-next-icon text-black"></span>
        </button>
    </div>
}
